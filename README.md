# CHIP8 emulator

A basic CHIP8 emulator. Passes the Does not support sound.

## Building

Build with CMake:
```bash
$ mkdir build
$ cd build
$ cmake ..
$ make
```

## Running

```bash
$ ./chip8 <path_to_rom>
```